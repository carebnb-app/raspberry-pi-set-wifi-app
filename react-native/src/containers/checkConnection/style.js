import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  indicator: {
    marginBottom: 30,
    marginTop: -150
  },

  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },

  inside: {
    width: '80%',
    alignItems: 'center',
    justifyContent: 'center'
  },

  text: {
    textAlign: 'center',
    fontSize: 18,
    marginBottom: 10
  },

  bold: {
    width: '90%',
    height: 100,
    marginTop: 20,
    marginBottom: 20,
    fontWeight: 'bold',
    textAlign: 'center',
    fontSize: 20
  }
})
